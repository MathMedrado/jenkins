pipeline {

    agent any
  
    stages {
        
      stage("build"){
        steps {
            sh "echo Sucesso"
        
      }
      stage("deploy"){
         steps {
                sh "echo  'deploy concluido'"
          }
      }
    }
}
